name: Delete Workflows

on: [workflow_dispatch]

jobs:
  do:
    runs-on: ubuntu-latest
    steps:
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2.0.3
        with:
          delete_workflow_pattern: hello-world-docker-ci.yml
          
